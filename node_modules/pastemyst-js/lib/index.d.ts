export interface CodeBlockMatchResult {
    language: string;
    code: string;
}
export declare class PasteMystInfo {
    id: string;
    link: string;
    createdAt: number;
    date: Date;
    code: string;
    expiresIn: string;
    language: string;
    constructor(id: string, link: string, createdAt: number, date: Date, code: string, expiresIn: string, language: string);
}
